{
  "husky": {
    "hooks": {
      "pre-commit": "rubocop && annotate",
      "pre-push": "rubocop && COVERAGE=true rspec --format progress"
    }
  },
  "dependencies": {
    "husky": "^1.0.0-rc.13"
  }
}
